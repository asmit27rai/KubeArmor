name: ci-release-operator-fork-test

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag or branch to test. Leave empty to use the selected branch."
        type: "string"
        required: false

permissions: read-all

env:
  PLATFORM: linux/amd64

jobs:
  kubearmor-operator-release-test:
    name: Build KubeArmor Operator Release Locally
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: "KubeArmor/go.mod"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@885d1462b80bc1c1c7f0b00334ad271f09369c55 # v2
        with:
          platforms: linux/amd64

      - name: Get Tag
        id: vars
        uses: actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b # v7
        with:
          script: |
            let tag = context.payload.inputs.tag;
            if (tag && tag.trim().length > 0) {
              await exec.exec('git', ['fetch', '--tags']);
              core.info(`Checking out ref: ${tag}`);
              await exec.exec('git', ['checkout', tag]);
            } else {
              const ref = context.ref;
              tag = ref === "refs/heads/main" ? "latest" : ref.replace(/^refs\/[^/]+\//, "");
            }
            core.setOutput('tag', tag);
            console.log(`Testing operator release build with tag: ${tag}`);

      - name: Install LLVM/Clang
        run: ./.github/workflows/install-llvm.sh

      - name: Install libbpf headers
        run: sudo apt-get update && sudo apt-get install -y libbpf-dev

      - name: Install bpftool
        run: ./.github/workflows/install-bpftool.sh

      - name: Generate vmlinux.h
        run: make -C BPF kernel_headers
        working-directory: KubeArmor

      - name: Generate BPF files
        run: go generate ./...
        working-directory: KubeArmor

      - name: Build KubeArmor Operator Images
        working-directory: ./pkg/KubeArmorOperator
        run: PLATFORM=$PLATFORM make docker-buildx TAG=${{ steps.vars.outputs.tag }} BUILD_MODE=--load
